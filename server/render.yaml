services:
  - type: web
    name: lets-bonk-relay
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: node relay.js
    envVars:
      - key: NODE_ENV
        value: production
